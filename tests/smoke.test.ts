/**
 * Smoke test to verify the test infrastructure is wired up correctly.
 * Real unit tests will be added in each module as they are built.
 */
describe("Test infrastructure", () => {
  it("should pass a basic assertion", () => {
    expect(1 + 1).toBe(2);
  });
});
