// Tailwind CSS v4 uses CSS-first configuration via app/globals.css.
// This file is kept for backward compatibility with tools that check
// for its existence, but v4 reads all configuration from the
// @theme block in CSS.
//
// See: https://tailwindcss.com/docs/v4-upgrade

const tailwindConfig = {};
export default tailwindConfig;
